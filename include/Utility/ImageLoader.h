#ifndef IMAGELOADER_H
#define IMAGELOADER_H

#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <GL/glut.h>

GLuint LoadImageFile(std::string);
GLuint LoadImage(char*);

#endif // IMAGELOADER_H
